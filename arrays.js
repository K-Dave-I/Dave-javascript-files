let list= ['boys','girls','broom'];
list[0]
console.log (list[0]);

list.unshift('home');
console.log(list);

list.push('bool');
console.log (list);

list.shift();
console.log(list);

list.pop();
console.log(list);

//object
let myDog = {
    name : "Jackson",
    age : "12",
    eyes : 2
}
console.log (myDog)